% ---------------------------------------------------------------------------
% Pacific Graphics 2026 manuscript draft
% ---------------------------------------------------------------------------
\documentclass{egpubl}
\usepackage{pg2026s}

\ConferenceSubmission

\usepackage{iftex}
\ifXeTeX
  \usepackage[UTF8]{ctex}
  \usepackage{graphicx}
\else
  \usepackage[T1]{fontenc}
  \usepackage{dfadobe}
  \usepackage{CJKutf8}
  \usepackage{graphicx}
\fi

\biberVersion
\BibtexOrBiblatex
\usepackage[backend=biber,bibstyle=EG,citestyle=alphabetic,backref=true]{biblatex}
\addbibresource{references.bib}

\electronicVersion
\PrintedOrElectronic
\usepackage{egweblnk}

\usepackage{xcolor}
\usepackage{amsmath,amssymb,booktabs,multirow,array}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{enumitem}
\usepackage{placeins}
\usepackage[percent]{overpic}

\hypersetup{hidelinks}

\newcommand{\traceplain}{XRA-GS}
\newcommand{\XRAGS}{\textbf{\traceplain}}
\newcommand{\sps}{SPS}
\newcommand{\gap}{GAP}
\newcommand{\adm}{ADM}
\newcommand{\rtwo}{R2-Gaussian}
\newcommand{\tracefull}{X-ray Attenuation-Aligned Gaussian Splatting}
\newcommand{\best}[1]{\textbf{#1}}
\newcommand{\second}[1]{\underline{#1}}
\newcommand{\third}[1]{#1}

\title[\traceplain\ for Sparse Tomographic View Synthesis]{\XRAGS: \tracefull\ for Sparse Tomographic View Synthesis}

\begin{document}
\ifXeTeX\else\begin{CJK*}{UTF8}{gbsn}\fi
\maketitle

\begin{abstract}
3D Gaussian Splatting has been extended to X-ray novel view synthesis, yet existing methods retain the gradient-driven densification rules from natural-image 3DGS without modification. Under sparse CT acquisition, these rules cause Gaussians to accumulate near high-contrast anatomical boundaries while interior tissues that determine X-ray attenuation remain under-represented. We present \XRAGS, an \tracefull\ framework that corrects this structural imbalance by redesigning the Gaussian evolution pipeline. XRA-GS introduces three components: a support-profile seeding strategy that initializes Gaussians from the coarse attenuation support estimated by FDK reconstruction; a geometry-aware pruning step that removes Gaussians concentrated near anatomical boundaries; and an adaptive density modulation step that applies position-dependent density refinement throughout training. Experiments on five organs under a sparse-view CT protocol demonstrate that XRA-GS achieves state-of-the-art novel view synthesis quality against six representative baselines from both visible-light sparse-view synthesis and X-ray CT methods.
\end{abstract}

\section{Introduction}
\label{sec:intro}

Computed tomography (CT) is an essential imaging technique for noninvasively examining the internal structure of an object, and is widely used in clinical diagnosis, image-guided intervention, and industrial inspection~\cite{feldkamp1984practical,andersen1984simultaneous,wang2008image}. During a CT acquisition, an X-ray source rotates around the object while a detector records multi-angle attenuation measurements, from which cross-sectional images or projections at unseen angles can be recovered through tomographic reconstruction. Reducing the number of acquired views is highly desirable, since it directly lowers radiation dose, shortens scan time, and broadens applicability to patients who cannot sustain long, stable acquisitions~\cite{li2023gloredi,wang2024reducing}. In this work, we study \emph{Sparse Tomographic View Synthesis}: from only a handful of X-ray projections, the model must predict the line-integrated attenuation observed at unseen acquisition angles, so that each synthesized projection remains consistent with the Beer--Lambert imaging geometry rather than merely visually similar to the inputs. A reliable solution supplies auxiliary views for anatomical localization, lesion-range inspection, and intraoperative planning under severely limited sampling, where conventional analytical reconstruction breaks down~\cite{willemink2019evolution}.

Visible-light and X-ray imaging are governed by fundamentally different rendering equations, and this difference dictates where representation capacity should reside. In visible-light 3DGS, a pixel is formed by front-to-back $\alpha$-compositing of an emission--absorption volume rendering integral~\cite{kerbl20233d,max1995optical}: the accumulated transmittance decays multiplicatively along the ray, so for opaque scenes the integrand is dominated by Gaussians near the first-hit surface while contributions from deeper Gaussians are progressively masked. The surface-clustered Gaussian layout that emerges under $\alpha$-compositing is therefore a direct consequence of this transmittance-induced concentration, and is consistent with the surface-aligned geometry that surface-oriented 3DGS variants exploit explicitly~\cite{guedon2024sugar,huang20242dgs}. In X-ray imaging, however, the detector reading follows the Beer--Lambert law~\cite{kak2001principles}: after taking the logarithm, the recorded quantity becomes a linear, order-independent line integral of the attenuation coefficient along the ray, with no transmittance decay and no occlusion. Consequently, every Gaussian on the path contributes on equal footing, and capacity must be distributed \emph{along the entire ray path} rather than collapsed onto any single layer. Existing X-ray Gaussian methods such as X-Gaussian, \rtwo, X-Field, and DGR have correctly replaced $\alpha$-compositing with a radiative line-integral renderer~\cite{cai2024xgaussian,hu2024r2,xfield2025,dgr2025}, and sparse-view 3DGS pipelines such as FSGS, CoR-GS, and DNGaussian further compensate for missing observations through depth or co-regularization priors~\cite{zhu2024fsgs,zhang2024cor,li2024dn}. Yet all of them inherit a Gaussian evolution rule that was originally designed for surface-centric $\alpha$-compositing~\cite{kerbl20233d,roessle2024densification}.

Crucially, the issue is not gradient-driven densification itself. Splitting or cloning Gaussians where the projection-error gradient is large is a sound idea, and our method retains exactly this mechanism. The issue is that under a line-integral renderer, the residual gradient is still concentrated at high-attenuation-contrast interfaces, such as bone--soft-tissue boundaries, which almost coincide with the high-color-contrast interfaces that drive visible-light densification. A na\"ive port therefore keeps spawning Gaussians at anatomical boundaries even though the rendering equation now asks for capacity \emph{along} the path. Under sparse views, this bias surfaces not as insufficient coverage but as a structural \emph{capacity misallocation}: a substantial fraction of Gaussians is spent on boundary-driven redundancy, while interior path segments that determine the attenuation integral remain under-represented. The top row of Fig.~\ref{fig:intro-compare} shows the natural fit between $\alpha$-compositing and surface-clustered Gaussians; the bottom row shows that when the same evolution rule is reused under line-integral rendering, capacity still collapses to boundaries and leaves the deep path empty. Fixing this requires \emph{augmenting}, not discarding, gradient-driven densification with attenuation-aligned decisions about where to seed, what to prune, and how to refine.

Motivated by this analysis, we propose \XRAGS, an \tracefull\ framework for Sparse Tomographic View Synthesis. Rather than appending auxiliary branches to a conventional densification backbone, \XRAGS\ keeps the gradient-driven backbone of standard 3DGS and adds three attenuation-aligned stages that act on \emph{where} Gaussians are seeded, retained, and refined, as illustrated in Fig.~\ref{fig:intro-compare}. Support-Profile Seeding (\sps) anchors initialization to a coarse FDK attenuation support~\cite{feldkamp1984practical}, so that Gaussians populate physically meaningful X-ray paths instead of starting from a contrast-biased prior; Geometry-aware Pruning (\gap) recycles Gaussians that accumulate near high-contrast anatomical interfaces during training, draining boundary-driven redundancy at its source; and Adaptive Density Modulation (\adm) applies position-dependent online refinement so that retained Gaussians carry interior attenuation rather than uniformly inflated density. Across diverse organs and view budgets, this attenuation-aligned evolution reallocates representation capacity from boundary redundancy to attenuation-relevant interior structures.

\begin{figure*}[t]
\centering
\IfFileExists{assets/fig/intro_fig_compare.png}{\includegraphics[width=0.97\textwidth]{assets/fig/intro_fig_compare.png}}{\fbox{\parbox[c][6cm][c]{0.95\textwidth}{\centering Placeholder: regenerate \texttt{assets/fig/intro\_fig\_compare.png} via \texttt{assets/scripts/intro\_fig\_compare\_image2\_call.py} once \texttt{IMAGE2\_API\_KEY} is provisioned.}}}
\caption{\textbf{Visible-light vs.\ X-ray Gaussian rendering and the capacity gap they impose.} (a) Visible-light $\alpha$-compositing concentrates capacity on visible surfaces, where front Gaussians occlude back ones. (b) X-ray line integrals weight every Gaussian along the ray equally, so na\"ive gradient-driven densification still clusters at attenuation boundaries; \XRAGS\ realigns the evolution rule through \sps, \gap, and \adm\ to anchor capacity along the path.}
\label{fig:intro-compare}
\end{figure*}

Our main contributions are summarized as follows:
\begin{itemize}[leftmargin=1.5em]
  \item We identify \emph{capacity misallocation} as the failure mode when sparse-view X-ray Gaussian methods inherit a visible-light evolution rule under a line-integral renderer: gradient-driven densification keeps clustering Gaussians at attenuation boundaries while interior path segments that determine the line integral remain starved of capacity.
  \item We propose \XRAGS, an \tracefull\ framework that augments standard gradient-driven densification with three coordinated attenuation-aligned stages---Support-Profile Seeding (\sps) for path-anchored initialization from coarse FDK support, Geometry-aware Pruning (\gap) for boundary-redundancy recovery, and Adaptive Density Modulation (\adm) for position-dependent online refinement---without modifying the X-ray renderer.
  \item Extensive experiments on multi-organ sparse-view CT show that \XRAGS\ delivers consistent gains in projection fidelity over state-of-the-art X-ray Gaussian and sparse-view 3DGS baselines, while reducing the number of Gaussians and training cost; ablations confirm that \sps, \gap, and \adm\ each contribute complementary capacity reallocation.
\end{itemize}

\section{Related Work}
\label{sec:related}

\subsection{Sparse-view Novel View Synthesis}

3D Gaussian Splatting (3DGS) represents a scene with anisotropic Gaussians and renders through differentiable rasterization~\cite{kerbl20233d}. Under sparse-view novel view synthesis, per-scene optimization methods diverge in how they explain failure: FSGS attributes degradation to insufficient coverage and augments densification with proximity-guided unpooling and depth priors~\cite{zhu2024fsgs}; CoR-GS treats the problem as unstable optimization and introduces dual-field co-regularization with co-pruning~\cite{zhang2024cor}; DNGaussian injects geometric priors through depth normalization~\cite{li2024dn}. A parallel feed-forward line moves geometry completion into large-scale pretraining~\cite{charatan2024pixelsplat,chen2024mvsplat,depthsplat2025,liu2025monosplat}. Explicit-and-hybrid representations such as K-Planes~\cite{fridovich2022kplanes} demonstrate that plane-decomposed spatial encodings provide continuous positional context at low cost; we adopt this encoding in \adm\ for position-dependent density refinement. Regardless of the direction, these methods share an error/gradient-driven densification backbone that prioritizes high-contrast surfaces. In CT, where each detector pixel integrates attenuation along the entire traversed path rather than sampling surface radiance, this evolutionary logic systematically misallocates capacity toward anatomical boundaries and underrepresents the interior structures that also participate in attenuation.

\subsection{X-ray Novel View Synthesis and Tomographic Reconstruction}

Learning-based sparse-view X-ray/CT methods fall into two families. Implicit methods extend neural attenuation fields with X-ray imaging structure: SAX-NeRF jointly models line dependencies and projection context~\cite{cai2024saxnerf}, while Geometry-Aware Attenuation Learning shifts toward population-level encoder-decoder inference~\cite{liu2025geometryaware}.

The explicit line redesigns Gaussians around X-ray attenuation. X-Gaussian introduced radiative Gaussians and differentiable X-ray rasterization~\cite{cai2024xgaussian}; \rtwo\ further aligned the representation with reconstruction consistency through integration-bias correction and differentiable voxelization~\cite{hu2024r2}. Subsequent work strengthens physical consistency: X-Field introduces material-aware ellipsoids and path segmentation for physics-aligned attenuation modeling~\cite{xfield2025}---its contribution targets the imaging equation rather than the evolution rule governing where Gaussians accumulate, leaving capacity misallocation from boundary-driven densification as an unaddressed bottleneck in sparse-view CT; DGR targets discretized voxel reconstruction~\cite{dgr2025}; GR-Gaussian addresses artifact suppression through graph-structured gradients~\cite{grgaussian2025}; X$^2$-Gaussian extends radiative Gaussians to 4D dynamic CT~\cite{x2gaussian2025}; and Layer-Based CT partitions volumes into anatomical depth layers for slice-wise reconstruction~\cite{layerbased2025}. These extensions target 4D dynamic CT, discretized voxel recovery, artifact-suppression graphs, or slice-wise volumetric reconstruction---objectives that fall outside the sparse-view novel-view projection protocol our experiments evaluate, and we therefore omit them from the main comparison. Our work belongs to the explicit radiative-Gaussian line, but its contribution is not another physical branch. Instead, we argue that even with a physically aligned renderer, sparse-view CT still suffers if Gaussian capacity allocation inherits the boundary-chasing logic of visible-light densification. This misallocation operates at three distinct stages: \emph{initialization} that ignores the X-ray attenuation support, \emph{boundary over-densification} during training, and \emph{undifferentiated point-wise density updates} in later refinement. \sps, \gap, and \adm\ each address one of these stages in turn, forming an attenuation-aligned Gaussian evolution pipeline rather than a modified rendering branch.

\section{Method}
\label{sec:method}

Figure~\ref{fig:intro-compare} already summarizes the core distinction at the pipeline level: \XRAGS\ does not keep stacking auxiliary branches on top of an existing densification backbone, but instead reorders how Gaussians are initialized, reclaimed, and refined. We now turn to the implementation level and show how this attenuation-aligned evolution pipeline is instantiated by \sps, \gap, and \adm\ on top of the radiative Gaussian backbone of \rtwo.

\subsection{Problem Formulation}

Let the sparse-view observation set be $\mathcal{P}=\{(I_v,\pi_v)\}_{v=1}^{V}$, where $I_v$ denotes the $v$-th X-ray projection and $\pi_v$ denotes its corresponding acquisition geometry. Given a coarse FDK reconstruction volume $V_{\text{FDK}}$ obtained from these observations, our goal is to learn an explicit radiative Gaussian representation in a per-case optimization setting so that unseen-angle target projections can be synthesized from only a few observed projections. Although this task is naturally coupled with volumetric density recovery, our primary optimization target remains novel-view quality in the projection domain.

\subsection{3DGS and X-ray Rendering Background}

To clarify that our problem is not simply ``adding another prior,'' we first contrast the pixel formation mechanisms of visible-light 3DGS and X-ray projection. In standard visible-light 3DGS, the color of a pixel is typically accumulated through front-to-back compositing of multiple splats ordered along the viewing ray:
\begin{equation}
\mathbf{C}(\mathbf{r})=\sum_{i=1}^{N_{\mathbf{r}}} T_i\,\alpha_i\,\mathbf{c}_i,\qquad
T_i=\prod_{j<i}(1-\alpha_j),
\end{equation}
where $\mathbf{r}$ is the camera ray, and $\alpha_i$ and $\mathbf{c}_i$ denote the opacity and radiance attributes of the $i$-th splat. The core intuition is that a pixel is formed by the accumulation of local surface contributions.

In X-ray imaging, however, a detector pixel corresponds more closely to the remaining energy of a single ray after traversing internal materials. Its physical measurement can be written as
\begin{equation}
\begin{aligned}
I(\mathbf{r}) &= I_0\exp\!\left(-\int_{\mathbf{r}}\mu(\mathbf{x})\,\mathrm{d}l\right),\\
-\log\frac{I(\mathbf{r})}{I_0} &= \int_{\mathbf{r}}\mu(\mathbf{x})\,\mathrm{d}l,
\end{aligned}
\end{equation}
where $\mu(\mathbf{x})$ is the attenuation coefficient at spatial location $\mathbf{x}$ and $\mathrm{d}l$ is the differential path length along the ray. In other words, an X-ray pixel is not a sum of surface colors; it is the integral of attenuation contributions from different materials along a single path. What matters is how much of each material occupies the path, rather than how strong the local response is at one boundary point.

We represent the scene as a Gaussian set $\mathcal{G}=\{g_i\}_{i=1}^{N}$, where each Gaussian is written as
\begin{equation}
g_i=(\mathbf{p}_i,\Sigma_i,\rho_i,\mathbf{c}_i),
\end{equation}
denoting the center, covariance, density, and appearance/radiative parameters, respectively. For any view $\pi_v$, the \rtwo-style radiative renderer can be written as
\begin{equation}
\hat{I}_v=\mathcal{R}(\mathcal{G};\pi_v).
\label{eq:renderer}
\end{equation}
Except for our new \sps, \gap, and \adm\ modules, the radiative rasterization, densification, and differentiable voxelization settings follow \rtwo~\cite{hu2024r2}. Importantly, X-Gaussian and \rtwo\ have already aligned the renderer with X-ray radiative projection. The mismatch we target lies mainly in the Gaussian evolution rule rather than in the imaging equation itself. In this baseline, Gaussians are still usually duplicated or split according to projection-error gradients. This is effective for improving surface coverage in natural scenes, but in CT it tends to keep pushing the evolving Gaussian set toward high-gradient anatomical boundaries. The model therefore uses the right X-ray renderer, yet still organizes Gaussians with a visible-light-style growth logic, which is precisely the issue our three modules address.

\subsection{Overall Framework}

As shown in Figure~\ref{fig:pipeline}, \XRAGS\ instantiates the attenuation-aligned Gaussian evolution pipeline as three sequential modules embedded into the explicit radiative Gaussian backbone of \rtwo. \sps\ seeds Gaussians into more relevant anatomical regions during initialization using the coarse FDK reconstruction. \gap\ then periodically reclaims locally redundant Gaussians after densification, preventing high-gradient boundaries from dominating Gaussian growth. Finally, \adm\ refines the density distribution of retained Gaussians based on position-aware context during later optimization. In short, we do not redesign X-ray physics; we reorganize the optimization loop itself into path anchoring, boundary de-redundancy, and local refinement. Algorithm~\ref{alg:training} summarizes the complete training procedure.

\begin{algorithm}[t]
\caption{\XRAGS\ Training Pipeline}\label{alg:training}
\begin{algorithmic}[1]
\Require Sparse projections $\mathcal{P}$, FDK volume $V_{\text{FDK}}$, iterations $T$
\Ensure Optimized Gaussian set $\mathcal{G}^{*}$
\State \textbf{Stage 1 --- Initialization (\sps):}
\State \quad Compute density-weighted sampling $p(\mathbf{x})$ from $V_{\text{FDK}}$ \hfill \textit{// Eq.~\eqref{eq:sps-profile}}
\State \quad Mix with uniform: $q(\mathbf{x}) \gets \alpha / |\Omega| + (1{-}\alpha)\,p(\mathbf{x})$ \hfill \textit{// Eq.~\eqref{eq:sps-mixture}}
\State \quad Sample $M$ centers; set scales and densities from $V_{\text{FDK}}$
\State \textbf{Stage 2 --- Main optimization loop:}
\For{$t = 1, \ldots, T$}
  \State Render $\hat{I}_v \gets \mathcal{R}(\mathcal{G}; \pi_v)$ for observed views \hfill \textit{// Eq.~\eqref{eq:renderer}}
  \State Compute loss $\mathcal{L}$ \hfill \textit{// Eq.~\eqref{eq:training-loss}}
  \State Update $\mathcal{G}$, \adm\ parameters via back-propagation
  \If{$t \bmod T_{\text{densify}} = 0$}
    \State Standard densification (clone / split)
    \If{$t \in [T_{\text{gap}}, T_{\text{gap,end}}]$}
      \State \textbf{\gap:} compute proximity $s_i$; prune by mask $m_i$ \hfill \textit{// Eqs.~\eqref{eq:gap-proximity}--\eqref{eq:gap-mask}; $T_{\text{gap}}\!=\!2\text{K}$, $T_{\text{gap,end}}\!=\!20\text{K}$}
    \EndIf
  \EndIf
  \If{$t \ge T_{\text{adm}}$}
    \State \textbf{\adm:} predict $\tilde{o}(\mathbf{x})$; modulate $\rho_{\text{final}}$ \hfill \textit{// Eq.~\eqref{eq:adm-density}}
  \EndIf
\EndFor
\State \Return $\mathcal{G}^{*} \gets \mathcal{G}$
\end{algorithmic}
\end{algorithm}

\begin{figure*}[t]
\centering
\IfFileExists{assets/fig/method_fig_pipeline.png}{%
\begin{overpic}[width=0.98\textwidth]{assets/fig/method_fig_pipeline.png}
  % --- module block labels ---
  \put(5,48){\colorbox{white!80!gray}{\footnotesize\textbf{Input}}}
  \put(1.5,34){\parbox{7em}{\centering\scriptsize Sparse X-ray\\[-1pt]Projections}}
  \put(1.5,18){\parbox{7em}{\centering\scriptsize FDK Coarse\\[-1pt]Recon.}}
  % SPS purple block
  \put(20,55){\colorbox{purple!15}{\color[HTML]{7B5CA6}\small\textbf{SPS}}}
  \put(18,44){\parbox{8em}{\centering\scriptsize Path-anchored\\[-1pt]initialization}}
  % Loop box label
  \put(37,88){\scriptsize\textit{Training loop}}
  % ADM orange block
  \put(38,55){\colorbox{orange!15}{\color[HTML]{E07B39}\small\textbf{ADM}}}
  \put(36,44){\parbox{8em}{\centering\scriptsize Density\\[-1pt]modulation}}
  % GAP teal block
  \put(55,55){\colorbox{teal!15}{\color[HTML]{3CA897}\small\textbf{GAP}}}
  \put(54,44){\parbox{8em}{\centering\scriptsize Redundancy\\[-1pt]recovery}}
  % Output
  \put(83,55){\colorbox{white!80!gray}{\footnotesize\textbf{Output}}}
  \put(82,34){\parbox{7em}{\centering\scriptsize Novel View\\[-1pt]Rendering}}
  % Legend
  \put(2,93){\colorbox[HTML]{7B5CA6}{\color{white}\scriptsize~Init~}}
  \put(11,93){\colorbox[HTML]{3CA897}{\color{white}\scriptsize~Pruning~}}
  \put(22,93){\colorbox[HTML]{E07B39}{\color{white}\scriptsize~Refinement~}}
\end{overpic}%
}{\fbox{\parbox[c][6cm][c]{0.95\textwidth}{\centering Placeholder: \texttt{assets/fig/method\_fig\_pipeline.png}}}}
\caption{\textbf{Overall framework of \XRAGS.} Given sparse X-ray projections $\{(I_v,\pi_v)\}_{v=1}^{V}$ and a coarse FDK reconstruction $V_{\text{FDK}}$, \sps\ (purple) performs path-anchored initialization along the attenuation support outside the optimization loop. An iterative training loop then interleaves \adm\ (orange, position-aware density modulation) and \gap\ (teal, gradient-aware redundancy recovery), supervised end-to-end by the rendering loss. Solid arrows denote data flow; dashed arrows denote gradient flow. The output is a novel-view X-ray projection together with the recovered tomographic volume.}
\label{fig:pipeline}
\end{figure*}

\subsection{Support-Profile Seeding (\sps)}

\sps\ initializes Gaussians in anatomically relevant regions using support-profile cues from the coarse FDK reconstruction. Initialization strongly influences the downstream optimization trajectory, yet neither of the standard 3DGS initializers fits CT well: uniform sampling cannot distinguish the anatomical importance of air, soft tissue, and bone, while SfM-based point-cloud initialization is unstable under gray-value overlap and weak local texture in X-ray projection~\cite{cai2024xgaussian}. Crucially, we do not use a vague spatial prior. Instead, we draw on three concrete signals contained in the FDK reconstruction: foreground anatomical support, low-frequency attenuation layout, and a coarse intensity hierarchy that differentiates air, soft tissue, and bony structures. The sparse-view CT literature also widely treats FDK/FBP reconstruction as a coarse CT volume with streak artifacts whose main value lies in global structure layout and useful frequency content rather than reliable fine detail~\cite{wang2022rfinet,li2023gloredi,li2025threedgrct,hu2024r2}. Based on these structural properties of the FDK reconstruction, we use the coarse density volume $V_{\text{FDK}}$ produced by FDK and define a density-weighted sampling distribution over a discrete voxel grid $\Omega$:
\begin{equation}
p(\mathbf{x})=\frac{\left(\max(V_{\text{FDK}}(\mathbf{x}),\epsilon)\right)^\gamma}{\sum_{\mathbf{x}'\in\Omega}\left(\max(V_{\text{FDK}}(\mathbf{x}'),\epsilon)\right)^\gamma},
\label{eq:sps-profile}
\end{equation}
where $\epsilon$ avoids zero probability in background regions and $\gamma$ controls sampling concentration.

Density weighting alone would over-concentrate Gaussians near the brightest bony structures and could mistake the strongest sparse-view FDK responses for precise geometry. We therefore mix uniform sampling with the support-profile distribution:
\begin{equation}
q(\mathbf{x})=\alpha\frac{1}{|\Omega|}+(1-\alpha)p(\mathbf{x}),\quad \mathbf{x}\in\Omega,
\label{eq:sps-mixture}
\end{equation}
and independently sample $M$ initial centers $\{\mathbf{x}_m\}_{m=1}^{M}$ from $q(\mathbf{x})$, where $M$ is the number of initial Gaussians. This design preserves global coverage while allocating more Gaussians to anatomical regions with meaningful attenuation support, without collapsing entirely onto dense bony boundaries. After sampling the locations, we initialize Gaussian densities from the FDK values and set scale parameters according to the local sampling density, yielding $50$K initial Gaussians. In all experiments, $\alpha$ and $\gamma$ are selected by a small local search and then fixed to avoid scene-specific manual tuning.

\begin{figure}[t]
\centering
\IfFileExists{assets/fig/method_fig_sps.png}{%
\begin{overpic}[width=\linewidth]{assets/fig/method_fig_sps.png}
  \put(1,92){\colorbox{white}{\footnotesize\textbf{(a)}}}
  \put(34,92){\colorbox{white}{\footnotesize\textbf{(b)}}}
  \put(67,92){\colorbox{white}{\footnotesize\textbf{(c)}}}
  % Equation overlay at bottom-right of panel (b)
  \put(40,5){\parbox{22em}{\centering\scriptsize$p(\mathbf{x})\!\propto\!V_{\text{FDK}}(\mathbf{x})^{\gamma},\quad q\!=\!\alpha/|\Omega|\!+\!(1\!-\!\alpha)p$}}
\end{overpic}%
}{\fbox{\parbox[c][5cm][c]{0.95\linewidth}{\centering Placeholder: \texttt{assets/fig/method\_fig\_sps.png}}}}
\caption{\textbf{Support-Profile Seeding (\sps).} (a) From the sparse projections, a coarse FDK volume is reconstructed; the horizontal attenuation profile along an anatomical scan line exposes the support-profile cue (purple curve). (b) \sps\ converts the attenuation magnitude into a density-weighted sampling map (viridis) and mixes it with a uniform prior, so capacity concentrates on anatomically relevant support while a small uniform term retains global coverage. (c) Compared with random initialization (gray dots), \sps\ produces an initial Gaussian set (purple dots) already aligned with the attenuation support before any optimization step.}
\label{fig:sps}
\end{figure}

\subsection{Geometry-aware Pruning (\gap)}

\gap\ periodically removes locally redundant Gaussians based on spatial proximity and gradient activity. In CT, gradient-driven densification often keeps duplicating Gaussians around bone-soft tissue boundaries because these regions produce larger projection-error gradients. However, high gradient does not always imply missing coverage; it may also indicate that the same anatomical boundary is being represented repeatedly by neighboring Gaussians. Instead of following the FSGS logic of adding support points in sparse regions~\cite{zhu2024fsgs}, \gap\ explicitly measures local crowding in world coordinates and uses it for redundancy recovery in over-densified regions.

Specifically, for each Gaussian $G_i$, let $\mathcal{N}_K(i)$ denote the index set of its $K$ nearest neighbors excluding itself. We compute the mean Euclidean distance to these neighbors as a proximity score:
\begin{equation}
s_i=\frac{1}{K}\sum_{j\in\mathcal{N}_K(i)}\|\mathbf{p}_i-\mathbf{p}_j\|_2.
\label{eq:gap-proximity}
\end{equation}
When $s_i$ falls below a threshold $\tau$, the Gaussian lies in an overly crowded region. To avoid deleting Gaussians that are still learning useful structure, we further introduce a gradient-activity filter. Let $\bar{g}_i$ denote the accumulated $L_1$ statistic of the positional gradient of this Gaussian over the current densification cycle. We define a retention mask $m_i\in\{0,1\}$ as
\begin{equation}
m_i=
\begin{cases}
1, & s_i\ge\tau \ \lor\ \bar{g}_i\ge\delta,\\
0, & \text{otherwise}.
\end{cases}
\label{eq:gap-mask}
\end{equation}
where $m_i=0$ means that the Gaussian is treated as a redundancy candidate in the current cycle. In implementation, \gap\ does not replace the original 3DGS densification; it is a periodic recovery step applied afterwards. We first generate candidate Gaussians through the standard duplication/splitting rule and then use $m_i$ to filter redundant ones. In each cycle, we prune at most a fraction $\beta_{\text{prune}}$ of the total Gaussians, and the module is triggered periodically from 2K to 20K iterations. To avoid suddenly making local support too sparse after pruning, we only shrink the covariance of newly retained Gaussians:
\begin{equation}
\Sigma_i^{\text{new}}=\eta\Sigma_i,\quad 0<\eta<1.
\end{equation}
\gap\ therefore regularizes the densification process by suppressing locally redundant Gaussians, preventing high-gradient boundaries from continuously monopolizing representation capacity.

\begin{figure}[t]
\centering
\IfFileExists{assets/fig/method_fig_gap.png}{%
\begin{overpic}[width=\linewidth]{assets/fig/method_fig_gap.png}
  \put(1,92){\colorbox{white}{\footnotesize\textbf{(a)}}}
  \put(34,92){\colorbox{white}{\footnotesize\textbf{(b)}}}
  \put(67,92){\colorbox{white}{\footnotesize\textbf{(c)}}}
  % Pruning criterion overlay
  \put(36,5){\parbox{22em}{\centering\scriptsize prune if $(s_i<\tau)\wedge(\bar{g}_i<\delta)$;\enspace remove $\leq\beta_{\text{prune}}\!\cdot\!N$}}
\end{overpic}%
}{\fbox{\parbox[c][5cm][c]{0.95\linewidth}{\centering Placeholder: \texttt{assets/fig/method\_fig\_gap.png}}}}
\caption{\textbf{Geometry-aware Pruning (\gap).} (a) After standard gradient-driven densification, Gaussians accumulate around high-attenuation boundaries, producing visible over-densification clusters. (b) For each Gaussian, \gap\ measures local crowding by the mean Euclidean distance to its $K$ nearest neighbors and combines it with the accumulated gradient activity over the current densification cycle: a Gaussian is flagged as a redundancy candidate (red $\times$) only if both KNN distance is below $\tau$ and gradient activity is below $\delta$; otherwise it is retained (green $\circ$). (c) After removing at most a fraction $\beta_{\text{prune}}$ of the redundant candidates, the cleaned distribution preserves the attenuation support while freeing capacity for under-served interior regions.}
\label{fig:gap}
\end{figure}

\subsection{Adaptive Density Modulation (\adm)}

\adm\ predicts position-dependent density offsets to refine the density distribution of retained Gaussians. Even after better initialization and redundancy control, density updates remain highly heterogeneous across anatomy: bone usually corresponds to higher attenuation, air regions in the lung should remain low-density, and soft tissue often requires smoother transitions. If all Gaussians share the same point-wise gradient update rule, the model struggles to express this spatial heterogeneity. We therefore encode Gaussian centers with K-Planes and use a lightweight decoder that predicts only density-related offsets and confidence gates, without rewriting Gaussian centers, covariances, or other attributes. The role of \adm\ is local density refinement rather than serving as the sole source of performance.

It is important to emphasize that, although \adm\ contains an MLP, it is not an offline pre-trained prediction head and does not rely on extra density supervision. Both the K-Planes features and the dual-head MLP parameters are updated only during optimization on the current case, and they are trained end-to-end through the main rendering loss. In this sense, \adm\ is an online joint-optimization branch that adaptively amplifies or suppresses density responses in different regions while the main radiative Gaussian backbone is being trained.

Given a position $\mathbf{x}$, we first project it onto normalized 2D coordinates $\Pi_{uv}(\mathbf{x})$ on three orthogonal planes and then bilinearly sample the corresponding feature maps:
\begin{equation}
f_{uv}(\mathbf{x})=\mathcal{B}\!\left(P_{uv},\Pi_{uv}(\mathbf{x})\right),\quad uv\in\{xy,xz,yz\},
\end{equation}
\begin{equation}
F(\mathbf{x})=\operatorname{concat}\!\big(f_{xy}(\mathbf{x}),f_{xz}(\mathbf{x}),f_{yz}(\mathbf{x})\big),
\end{equation}
where $\mathcal{B}(\cdot)$ denotes bilinear sampling, $P_{xy}$, $P_{xz}$, and $P_{yz}$ are the three orthogonal feature planes, and $\operatorname{concat}(\cdot)$ denotes channel-wise concatenation. K-Planes encoding was originally introduced for explicit spatiotemporal radiance fields and provides continuous spatial context at low cost~\cite{fridovich2022kplanes}. Based on this encoding, a dual-head MLP predicts a density offset $\Delta\sigma(\mathbf{x})$ and a confidence gate $g(\mathbf{x})$:
\begin{equation}
\big(\Delta\sigma(\mathbf{x}),g(\mathbf{x})\big)=h(F(\mathbf{x})).
\end{equation}
We then center the offsets by the batch mean and apply a view-dependent scaling:
\begin{equation}
\tilde{o}(\mathbf{x})=\beta(t)\,s_{\text{view}}\,r_{\max}\,g(\mathbf{x})\cdot\big(\Delta\sigma(\mathbf{x})-\overline{\Delta\sigma}\big),
\end{equation}
where $\overline{\Delta\sigma}$ is the mean density offset of the current batch, $r_{\max}$ is the maximum modulation amplitude, and $s_{\text{view}}$ is a view-count scaling factor (concretely, $s_{\text{view}}\!=\!0.5,\,0.7,\,1.0$ for 2, 3, and 4 input views, respectively). To prevent modulation from disrupting basic geometric convergence early in training, we use a warm-up/hold/decay schedule for $\beta(t)$:
\begin{equation}
\beta(t)=
\begin{cases}
\frac{t}{t_w}, & 0\le t<t_w,\\
1, & t_w\le t<t_h,\\
\exp\!\big(-\frac{t-t_h}{\kappa}\big), & t\ge t_h.
\end{cases}
\end{equation}
where $t_w$ marks the end of the warm-up phase, $t_h$ marks the end of the hold phase, and $\kappa$ is the exponential decay time constant. The final density is
\begin{equation}
\rho_{\text{final}}(\mathbf{x})=\rho_{\text{base}}(\mathbf{x})\cdot\big(1+\tilde{o}(\mathbf{x})\big).
\label{eq:adm-density}
\end{equation}
Therefore, \adm\ learns not a global density boost, but a spatial-context-aware relative correction that better matches the locally continuous yet regionally heterogeneous structure of CT data.

\begin{figure}[t]
\centering
\IfFileExists{assets/fig/method_fig_adm.png}{%
\begin{overpic}[width=\linewidth]{assets/fig/method_fig_adm.png}
  \put(1,92){\colorbox{white}{\footnotesize\textbf{(a)}}}
  \put(34,92){\colorbox{white}{\footnotesize\textbf{(b)}}}
  \put(67,92){\colorbox{white}{\footnotesize\textbf{(c)}}}
  % Core formula overlay
  \put(36,4){\parbox{22em}{\centering\scriptsize$\rho_{\text{final}}\!=\!\rho_{\text{base}}\!\cdot\!(1\!+\!\tilde{o}),\quad\tilde{o}\!=\!\beta(t)\,g(\mathbf{x})\,(\Delta\sigma(\mathbf{x})\!-\!\overline{\Delta\sigma})$}}
\end{overpic}%
}{\fbox{\parbox[c][5cm][c]{0.95\linewidth}{\centering Placeholder: \texttt{assets/fig/method\_fig\_adm.png}}}}
\caption{\textbf{Adaptive Density Modulation (\adm).} (a) Three orthogonal K-Planes feature maps (viridis) encode position-dependent spatial context for every Gaussian center. (b) The concatenated K-Planes feature is decoded by a lightweight dual-head MLP that predicts a density offset $\Delta\sigma(\mathbf{x})$ (orange) and a confidence gate $g(\mathbf{x})\in[0,1]$ (slate); the modulated density combines them through $\rho_{\text{final}}=\rho_{\text{base}}\cdot(1+\tilde{o})$. (c) The resulting density difference map (inferno) shows that \adm\ amplifies density in regions where the attenuation support requires fine detail and damps modulation where evidence is uncertain.}
\label{fig:adm}
\end{figure}

\subsection{Training Loss}

The three modules of \XRAGS\ and the radiative rendering backbone of \rtwo\ form a single end-to-end training pipeline. During training, we supervise reprojection results on the observed views with the following overall loss:
\begin{equation}
\mathcal{L}=\mathcal{L}_{1}+\lambda_{\text{dssim}}\mathcal{L}_{\text{dssim}}+\lambda_{\text{3DTV}}\mathcal{L}_{\text{3DTV}}+\lambda_{\text{pTV}}\mathcal{L}_{\text{pTV}},
\label{eq:training-loss}
\end{equation}
where
\begin{align}
\mathcal{L}_{1} &= \frac{1}{V}\sum_{v=1}^{V}\|I_v-\hat{I}_v\|_1, \\
\mathcal{L}_{\text{dssim}} &= \frac{1}{V}\sum_{v=1}^{V}\operatorname{DSSIM}(I_v,\hat{I}_v), \\
\mathcal{L}_{\text{3DTV}} &= \sum_{i=1}^{N}\sum_{d\in\{x,y,z\}}\left|\nabla_d \rho_i\right|, \\
\mathcal{L}_{\text{pTV}} &= \sum_{p\in\{xy,xz,yz\}}\omega_p(t)\operatorname{TV}(P_p).
\end{align}
Here, $\omega_p(t)$ is the time-varying regularization weight for the $p$-th feature plane; it follows the same warm-up/hold/decay schedule as $\beta(t)$, ramping linearly from $0$ to $\omega_0$ over the first $t_w$ iterations, holding at $\omega_0$ until $t_h$, then decaying exponentially with time constant $\kappa$. $\mathcal{L}_{1}$ and $\mathcal{L}_{\text{dssim}}$ jointly constrain projection reconstruction quality, $\mathcal{L}_{\text{3DTV}}$ encourages spatial smoothness in volumetric density, and $\mathcal{L}_{\text{pTV}}$ imposes planar total-variation regularization on the K-Planes features. During training, the plane features and MLP parameters of \adm\ are jointly updated with the backbone Gaussian parameters under the same loss, so density modulation is learned online rather than fixed in advance. Since \sps\ only changes initialization, \gap\ is triggered periodically during training, and \adm\ applies lightweight density modulation, the basic X-ray rendering process at inference remains unchanged and the extra cost is concentrated in training.

\section{Experiments}
\label{sec:experiments}

\subsection{Experimental Setup}

\textbf{Data and task setting.} We evaluate on the five-organ dataset from the X-Gaussian
repository~\cite{cai2024xgaussian}: Chest (LIDC-IDRI) and Head, Abdomen, Foot, and Pancreas
(open scientific visualization dataset). Following the NAF/X-Gaussian preprocessing and projection
protocol, we construct sparse-view settings by retaining only 2, 3, or 4 input projections per
scene. Models are trained exclusively on observed projections and evaluated on held-out novel
angles.

\textbf{Baselines.} We compare against six methods in two groups. \emph{Direct X-ray/CT
baselines:} X-Gaussian~\cite{cai2024xgaussian} (radiative Gaussian splatting),
\rtwo~\cite{hu2024r2} (reconstruction-consistency regularization), and
X-Field~\cite{xfield2025} (material-aware implicit modeling) represent the state of the art for
X-ray novel view synthesis. \emph{Natural-scene sparse-view 3DGS references:}
CoR-GS~\cite{zhang2024cor}, DNGaussian~\cite{li2024dn}, and FSGS~\cite{zhu2024fsgs} characterize
how gradient-driven densification degrades under tomographic projection. X-Field is re-evaluated
under our identical protocol and denoted as X-Field$^*$ in the result tables and figures.

\textbf{Metrics.} Following the official X-Gaussian evaluation script, PSNR2D and SSIM2D are
computed on $[0,1]$-normalized projections averaged over all held-out views of each scene and then
over the five organs. PSNR2D is $20\log_{10}(1/\sqrt{\text{MSE}})$; SSIM2D uses an
$11\!\times\!11$ Gaussian window ($\sigma\!=\!1.5$, $C_1\!=\!0.01^2$, $C_2\!=\!0.03^2$).

\textbf{Implementation details.} \sps\ uses $\alpha\!=\!0.2$, $\gamma\!=\!1.0$, and 50K initial
Gaussians. \gap\ uses $K\!=\!5$ neighbors, threshold $\tau\!=\!0.015$, maximum pruning ratio
$\beta_{\text{prune}}\!=\!0.02$, and gradient threshold $\delta\!=\!0.0002$. \adm\ uses
K-Planes at resolution $64\!\times\!32$ and activates after 15K iterations. All models are
trained for 30K iterations.

\subsection{Main Results}

Table~\ref{tab:comparison} reports PSNR2D across all five organs and three sparsity levels.
\XRAGS\ achieves the best five-organ average at every sparsity level. Gains over \rtwo\ are
$+0.11/+0.39/+0.07$\,dB under 2/3/4-view settings; gains over X-Field$^*$ are
$+0.87/+3.25/+2.79$\,dB. The most pronounced margin occurs at 3 views, consistent with our
central argument: capacity misallocation is most directly addressable when projection evidence is
sufficient to guide redistribution yet too limited for standard densification to converge to a
spatially consistent solution.

\input{assets/tables/tab_experiment_comparison.tex}

Table~\ref{tab:comparison-ssim} reports SSIM2D averages for the three methods with accessible
logs under our unified protocol. \XRAGS\ leads at 2- and 3-view settings and ties \rtwo\ at the
third decimal place in 4-view, confirming the same trend as PSNR2D.

\input{assets/tables/tab_experiment_comparison_ssim.tex}

Figure~\ref{fig:qual-main} provides qualitative comparisons across all five organs under the
2-view setting. The natural-scene sparse-view baselines (CoR-GS, DNGaussian, FSGS) produce
characteristic boundary leakage and streak artifacts under tomographic projection; the direct
X-ray baselines (X-Gaussian, \rtwo, X-Field$^*$) preserve global attenuation structure more
faithfully. \XRAGS\ further reduces background diffusion and local over-accumulation near
high-contrast anatomical boundaries, consistent with the quantitative gains in
Table~\ref{tab:comparison}.

\begin{figure*}[t]
\centering
\IfFileExists{assets/fig/experiment_fig_qual_main.png}{%
  \includegraphics[width=\textwidth]{assets/fig/experiment_fig_qual_main.png}%
}{\fbox{\parbox[c][7cm][c]{0.95\textwidth}{\centering\small
  Placeholder: \texttt{assets/fig/experiment\_fig\_qual\_main.png}\\[4pt]
  5 organs (rows) $\times$ 8 cols (GT / CoR-GS / DNGaussian / FSGS / X-Field$^*$ / X-Gaussian /
  \rtwo / \XRAGS) + error map col, 2-view setting
}}}
\caption{\textbf{Qualitative comparison on five organs, 2-view setting.} Rows: Chest, Head,
Abdomen, Foot, Pancreas (top to bottom). Columns: GT, CoR-GS, DNGaussian, FSGS, X-Field$^*$,
X-Gaussian, \rtwo, and \XRAGS\ (Ours, last column). Natural-scene references produce streak
artifacts and boundary leakage; direct X-ray baselines preserve global attenuation structure;
\XRAGS\ further suppresses local over-accumulation and background diffusion. Rightmost column:
error maps (inferno, shared vmax). Red boxes mark regions enlarged in
Figure~\ref{fig:qual-detail-consistency}.}
\label{fig:qual-main}
\end{figure*}

Figure~\ref{fig:qual-detail-consistency} provides two complementary analyses. The upper portion
enlarges the Chest and Foot regions marked in Figure~\ref{fig:qual-main}, isolating boundary and
structure recovery at the local scale. The lower portion evaluates a Chest 2-view case at three
novel angles ($0^\circ$, $45^\circ$, $90^\circ$), confirming that the advantage of \XRAGS\ over
\rtwo\ is stable across viewing directions rather than concentrated at a single angle.

\begin{figure*}[t]
\centering
\IfFileExists{assets/fig/experiment_fig_qual_detail_consistency.png}{%
  \includegraphics[width=\textwidth]{assets/fig/experiment_fig_qual_detail_consistency.png}%
}{\fbox{\parbox[c][7cm][c]{0.95\textwidth}{\centering\small
  Placeholder: \texttt{assets/fig/experiment\_fig\_qual\_detail\_consistency.png}\\[4pt]
  Upper: zoom-in Chest + Foot (4 cols: GT / \rtwo / X-Gaussian / \XRAGS)\\
  Lower: Chest 2-view at $0^\circ$/$45^\circ$/$90^\circ$ (3 rows $\times$ 4 cols)
}}}
\caption{\textbf{Local detail and cross-view consistency.} \emph{Upper half:} Zoom-ins from the
red boxes in Figure~\ref{fig:qual-main} for Chest (left) and Foot (right), comparing GT,
\rtwo, X-Gaussian, and \XRAGS. \XRAGS\ reduces streak spread and boundary diffusion in both
regions. \emph{Lower half:} A Chest 2-view case rendered at $0^\circ$, $45^\circ$, and
$90^\circ$. The quality advantage of \XRAGS\ persists across all three angles, indicating
consistent global attenuation reconstruction rather than angle-specific improvement.}
\label{fig:qual-detail-consistency}
\end{figure*}

\noindent\textbf{Same-protocol comparison with X-Field.} The X-Field$^*$ row in
Table~\ref{tab:comparison} confirms that the gains of \XRAGS\ are not an artifact of protocol
asymmetry. Re-evaluated under our identical 2/3/4-view setting, X-Field$^*$ trails \XRAGS\ by a
consistent average margin across all sparsity levels.

\subsection{Module and Initialization Analysis}

Table~\ref{tab:component_ablation} reports a progressive ablation starting from the \rtwo\
backbone. \sps\ raises the 2-view average PSNR2D from 21.27 to 21.44\,dB, establishing that
path-anchored initialization provides a meaningful head start in the most under-determined regime.
\gap\ delivers the largest PSNR2D increment, improving the 3-view average from 28.01 to
28.22\,dB by reclaiming capacity from over-densified boundary regions. The final \XRAGS\ row
matches or improves the available PSNR2D measurements across all view counts, while intermediate
SSIM2D values are pending from the experiment logs.

\input{assets/tables/tab_experiment_component.tex}

Figure~\ref{fig:ablation-visual} translates the progressive ablation into local visual evidence.
Residual maps $|\hat{I} - I_\text{gt}|$ are shown below each prediction. This visual analysis is
used to inspect where the progressive modules change the reconstruction, while the corresponding
intermediate SSIM2D values remain pending from the experiment logs.

\begin{figure*}[t]
\centering
\IfFileExists{assets/fig/experiment_fig_ablation_visual.png}{%
  \includegraphics[width=\textwidth]{assets/fig/experiment_fig_ablation_visual.png}%
}{\fbox{\parbox[c][6cm][c]{0.95\textwidth}{\centering\small
  Placeholder: \texttt{assets/fig/experiment\_fig\_ablation\_visual.png}\\[4pt]
  Chest 3-view --- top: GT / Baseline / +\sps\ / +\sps+\gap\ / Full \XRAGS\\
  bottom: residual maps $|\hat{I}-I_\text{gt}|$ (inferno); $\Delta$SSIM2D annotated under each col
}}}
\caption{\textbf{Progressive module ablation, Chest 3-view.} Top row: rendered projections for
GT, Baseline (\rtwo), +\sps, +\sps+\gap, and full \XRAGS. Bottom row: residual maps
$|\hat{I}-I_\text{gt}|$ (inferno, shared vmax with Figure~\ref{fig:qual-main}). Quantitative
intermediate SSIM2D values are pending from experiment logs and are therefore not used for module-level claims here.}
\label{fig:ablation-visual}
\end{figure*}

Figure~\ref{fig:spatial-distribution} is reserved for visualizing the cumulative effect of the
three modules on Gaussian spatial distribution once the corresponding Gaussian-center and count
logs are available. Until those logs are confirmed, the mechanism is evaluated through the
available quantitative ablation in Table~\ref{tab:component_ablation}.

\begin{figure*}[t]
\centering
\IfFileExists{assets/fig/experiment_fig_spatial_distribution.png}{%
  \includegraphics[width=\textwidth]{assets/fig/experiment_fig_spatial_distribution.png}%
}{\fbox{\parbox[c][5cm][c]{0.95\textwidth}{\centering\small
  Placeholder: \texttt{assets/fig/experiment\_fig\_spatial\_distribution.png}\\[4pt]
  4 panels: Uniform Init ($\approx$50K) $\to$ +\sps\ ($\approx$42K) $\to$
  +\sps+\gap\ ($\approx$38K) $\to$ Full \XRAGS\ ($\approx$35K); attenuation profile strip above
}}}
\caption{\textbf{Gaussian spatial distribution evolution.} Four panels (left to right): uniform
initialization, after \sps, after \gap\ pruning, and full \XRAGS, for a representative Chest
organ. Top strip: FDK attenuation profile (magma shading). The final figure will use logged
Gaussian centers and counts rather than schematic estimates.}
\label{fig:spatial-distribution}
\end{figure*}

Table~\ref{tab:init_viewcount} (left) isolates the initialization strategy. SPS-guided seeding
outperforms random initialization at all sparsity levels; the full \XRAGS\ further improves upon
SPS-only through \gap\ and \adm, confirming that path-anchored initialization and capacity
recovery are complementary operations.

\input{assets/tables/tab_experiment_init_viewcount.tex}

\subsection{View-count Analysis}

Table~\ref{tab:init_viewcount} (right) reports the view-count trend for four representative
methods. \XRAGS\ achieves the highest five-organ average PSNR2D at every sparsity level. The
relative gain over \rtwo\ is largest at 3 views, where capacity misallocation is most directly
observable; at 4 views the reconstruction-consistency regularization in \rtwo\ partially closes
the gap, consistent with the applicability boundary discussed in
Section~\ref{sec:discussion}.

\subsection{Computational Cost}

Table~\ref{tab:efficiency} is reserved for training and inference cost under the 3-view setting.
The current manuscript keeps the table structure in place, but Time, final Gaussian count, GPU
memory, and FPS must be filled from experiment logs before making quantitative efficiency claims.

\input{assets/tables/tab_experiment_efficiency.tex}

\subsection{Hyperparameter Sensitivity}
\label{sec:hparam}

Figure~\ref{fig:hparam} is reserved for local sweeps of four key hyperparameters in the Chest
3-view setting: the \sps\ mixture coefficient $\alpha$, the \gap\ proximity threshold $\tau$, the
\gap\ pruning ratio $\beta_\text{prune}$, and the \adm\ activation iteration. The default
configuration ($\alpha\!=\!0.2$, $\tau\!=\!0.015$, $\beta_\text{prune}\!=\!2\%$, \adm\ activation
at 15K) will be assessed once the sweep metrics are available from the experiment logs.

\begin{figure}[t]
\centering
\IfFileExists{assets/fig/experiment_fig_hparam.png}{%
  \includegraphics[width=\linewidth]{assets/fig/experiment_fig_hparam.png}%
}{\fbox{\parbox[c][6cm][c]{0.95\linewidth}{\centering\small
  Placeholder: \texttt{assets/fig/experiment\_fig\_hparam.png}\\[4pt]
  $2\!\times\!2$ panels: (a)~\sps\ $\alpha$;\enspace (b)~\gap\ $\tau$;\enspace
  (c)~\gap\ $\beta_\text{prune}$;\enspace (d)~\adm\ activation\\
  Primary y-axis: SSIM2D (solid Crimson); secondary: PSNR2D (dashed); $\bullet$ = default
}}}
\caption{\textbf{Hyperparameter sensitivity, Chest 3-view.} Each panel sweeps one hyperparameter
with all others fixed at the default (marked $\bullet$). Solid Crimson: SSIM2D$\uparrow$;
dashed: PSNR2D$\uparrow$. (a)~\sps\ mixture coeff.\ $\alpha\!\in\!\{0.0,0.25,0.5,0.75,1.0\}$;
(b)~\gap\ threshold $\tau$; (c)~\gap\ pruning ratio
$\beta_\text{prune}\!\in\!\{0,1,2,5,10\}\%$; (d)~\adm\ activation iteration (fraction of 30K iters).
The final sweep figure will be populated from experiment logs.}
\label{fig:hparam}
\end{figure}

\FloatBarrier

\subsection{Discussion and Limitations}
\label{sec:discussion}

The experiments confirm that in the moderately sparse regime, the primary bottleneck is capacity
allocation rather than Gaussian count. Gains are most pronounced at 3 views, where sufficient
projection evidence exists for path-anchored initialization and boundary de-redundancy to take
effect, yet too little evidence for standard densification to converge to a spatially consistent
solution.

Two boundaries constrain the current method. At 2 views the FDK reconstruction used by \sps\ is
insufficiently reliable, so the initialization advantage partially erodes. At 4 views the
reconstruction-consistency regularization in \rtwo\ is strong enough to compensate for
boundary-chasing effects, reducing the relative advantage of \XRAGS. Figure~\ref{fig:failure-cases}
illustrates both failure modes: in Foot 2-view and Pancreas 2-view, fine-grained high-frequency
detail remains unrecoverable when projection evidence is fundamentally insufficient, regardless of
initialization or capacity management.

\begin{figure*}[t]
\centering
\IfFileExists{assets/fig/experiment_fig_failure_cases.png}{%
  \includegraphics[width=\textwidth]{assets/fig/experiment_fig_failure_cases.png}%
}{\fbox{\parbox[c][5.5cm][c]{0.95\textwidth}{\centering\small
  Placeholder: \texttt{assets/fig/experiment\_fig\_failure\_cases.png}\\[4pt]
  2 rows: Foot 2-view (top) / Pancreas 2-view (bottom)\\
  4 cols: GT / \rtwo / \XRAGS\ / error map (inferno); failure desc.\ inset right ($\leq$25 words)
}}}
\caption{\textbf{Representative failure cases, 2-view setting.} Rows: Foot (top), Pancreas
(bottom). Columns: GT, \rtwo, \XRAGS, and \XRAGS\ error map (inferno, shared vmax with
Figure~\ref{fig:qual-main}). Zoom-in boxes mark the primary failure region. \XRAGS\ reduces
overall error spread relative to \rtwo\ but cannot resolve fine structural detail when projection
evidence is fundamentally absent. Failure descriptions ($\leq$25 words) are inset at the right of
each row.}
\label{fig:failure-cases}
\end{figure*}

Several directions remain open. First, \sps\ relies on FDK quality, which degrades sharply below
two views; learned or uncertainty-aware initializers could relax this constraint. Second, the
\gap\ threshold and pruning ratio are fixed organ-agnostically; organ-adaptive scheduling may
improve per-organ performance. Third, all experiments assume parallel-beam geometry; extension to
cone-beam and dynamic CT acquisition is left for future work. Additional per-organ SSIM2D
breakdowns and single-module ablations will be included only after the corresponding same-protocol
logs are confirmed.

\section{Conclusion}
\label{sec:conclusion}

\XRAGS\ demonstrates that visible-light densification rules impose a systematic capacity-allocation failure in transmissive X-ray imaging that persists even after the rendering equation is physically corrected. By intervening at three stages of Gaussian evolution---path-anchored initialization (\sps), boundary redundancy recovery (\gap), and position-dependent local refinement (\adm)---the framework redirects representation capacity from over-densified anatomical boundaries toward the interior attenuation-relevant structures that conventional gradient-driven growth neglects. Across a five-organ benchmark with 2/3/4 input views against six representative baselines, \XRAGS\ achieves the best average PSNR2D at every sparsity level, with SSIM2D reported for methods whose same-protocol logs are available. The strongest PSNR2D improvement appears in the moderately sparse 3-view regime, where sufficient projection evidence allows capacity reallocation to take effect.

\printbibliography
\ifXeTeX\else\end{CJK*}\fi
\end{document}
